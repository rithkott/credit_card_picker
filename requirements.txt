# Runtime deps for the Vercel Python function (api/index.py -> server/app.py).
# Local development instead uses server/requirements.txt (adds uvicorn/httpx).
fastapi
pyyaml
# Statement parsing (plan 12): multipart uploads, PDF text layer, fuzzy matcher.
python-multipart
pdfplumber
rapidfuzz
# Semantic matcher layer 6 (plan 13): local MiniLM transformer runtime.
numpy
tokenizers
onnxruntime
