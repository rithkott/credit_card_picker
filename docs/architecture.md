# System Architecture

Current state of what's actually built: dataset layer + validation pipeline + optimization engine + the product web UI (React SPA, FastAPI wrapper with server-side statement parsing, and the Vercel deployment serving both — live at https://creditcardpicker.vercel.app).

> **Maintenance rule:** this diagram must be updated in the same change as any edit to `data/schema/card.schema.json`, the `data/meta/` registries, `scripts/validate_cards.py`, `scripts/optimize.py`, `.github/workflows/validate-data.yml`, or the repo's data layout. See `CLAUDE.md`.

```mermaid
flowchart TB

subgraph SOURCE["📁 Source of truth — hand-curated YAML, one file per card"]
    FORM["<b>tools/card-entry-form.html</b><br/><i>browser form (no build/server) that emits schema-valid YAML —<br/>mirrors the validator's checks live, embeds registry keys<br/>(must be kept in sync when registries change)</i>"]
    CARD["data/cards/&lt;issuer&gt;/&lt;card-id&gt;.yaml<br/><i>one file per card: small reviewable PRs,<br/>per-card git history, YAML comments carry 'why' notes</i>"]
    FORM -->|"copy/download YAML into"| CARD
end

subgraph SCHEMA["📐 data/schema/card.schema.json — every card decomposes into blocks needing different math"]
    IDENTITY["<b>id / name / issuer / network / portal</b><br/><i>id = filename, issuer = directory —<br/>enforced so files can't drift from their paths.<br/>portal (optional, required iff any reward is portal_only) names<br/>the statement-descriptors key those rates book through (use is<br/>assumed — you book through the portal of the card you hold) —<br/>card-level, not issuer-level (Citi AA cards use aadvantage_hotels,<br/>not citi_travel). unlocks_transfers (optional) marks a transfer<br/>gateway: holding it unlocks its program's optimistic_cpp for every<br/>same-program card in the portfolio (Sapphire, Strata Premier/Elite,<br/>Autograph Journey)</i>"]
    CURRENCY["<b>currency: type + program</b><br/><i>declares what a card earns (cash vs points)<br/>and which program prices those points —<br/>cards never embed their own point values</i>"]
    BASE["<b>base_rate (+ base_rate_conditional)</b><br/><i>catch-all earn rate; rewards are written as<br/>exceptions layered over it, never exhaustive lists.<br/>base_rate_conditional stores a gated boost (Apple Card 2%<br/>via Apple Pay vs 1% physical) — stored, unscored</i>"]
    CATREW["<b>category_rewards[]</b><br/><i>spend-proportional elevated rates.<br/>cap = spend ceiling + fallback_rate (issuers limit 5-6% rates);<br/>shared_cap_id = several entries drawing ONE combined pool<br/>('2x gas + groceries on the first $5k/yr combined');<br/>rotation = quarterly categories + activation (so non-activation<br/>can be discounted); choice = options list the cardholder picks<br/>from (optimizer expands into per-option variants);<br/>portal_only (portal prices run high); requires_enrollment<br/>(rates that pay 1% unless actively enrolled);<br/>conditional_rate = membership/payment-method-gated boost<br/>('5% with Walmart+, else 3%') — baseline scored, boost stored</i>"]
    MERREW["<b>merchant_rewards[]</b><br/><i>same shape, keyed to a named merchant<br/>('5% at Amazon') — beats category when both match;<br/>also carries conditional_rate</i>"]
    CREDITS["<b>credits[]</b><br/><i>fixed-value, use-it-or-lose-it, periodic — NOT scaled by spend.<br/>amount_usd or amount_points (anniversary point drops, valued via<br/>the card's program); kind: in_kind marks estimated-value non-cash<br/>benefits (free nights, companion certificates) that always take a<br/>capture haircut; unlock_spend_usd gates spend-triggered credits;<br/>requires_enrollment + expires record capture drag and promo ends.<br/>Every credit must carry a gate (plan 07): usage_keys[] names the<br/>usage-questions items whose user confirmation unlocks it (anyOf —<br/>unconfirmed ⇒ $0, a DoorDash credit is worthless to a non-user);<br/>category alone = generic spend-offset gated by real category spend;<br/>automatic: true = anniversary-style, pays with no merchant action<br/>(the only full-face path, exclusive of the other two).<br/>portal_only: true marks a credit redeemable only via an issuer<br/>travel/hotel portal (Amex FHR, Chase Travel/The Edit, Cap One<br/>Travel, Citi Travel, Delta Stays, United Hotels) — the optimizer<br/>counts at most ONE portal credit per portfolio (a user books<br/>through one portal), so stacked cards' portal credits don't inflate<br/>net; per-card display is unaffected.<br/>realistic_capture_rate_note is required because issuers price<br/>annual fees against sticker credit value most users never capture</i>"]
    SUB["<b>signup_bonus</b><br/><i>one-time, year-one only; value mixes points and/or usd;<br/>tiers[] add tranches at higher cumulative spend ('70k after $3k,<br/>+20k after $5k total'); first_year_match models Discover's<br/>Cashback Match (valued as the card's own year-1 earnings).<br/>limited_time + expires track elevated promos so an expired<br/>offer can't silently inflate recommendations</i>"]
    FEES["<b>fees</b><br/><i>annual_fee_usd + first_year_waived + foreign_transaction_pct —<br/>the costs every reward must be netted against</i>"]
    APPROVAL["<b>approval</b><br/><i>credit_tier (building→excellent) + estimated_min_score + notes.<br/>exists so no card is ever recommended that the user<br/>can't actually get approved for; 'building' makes<br/>secured/credit-builder cards first-class</i>"]
    CLOSED["<b>closed_loop (optional)</b><br/><i>marks store cards usable ONLY at listed merchants —<br/>expresses the restriction, not the reward, so store cards<br/>can still be recommended when spend concentrates there</i>"]
    RELBOOST["<b>relationship_boost (optional)</b><br/><i>banking-relationship earn boosts (BofA Preferred<br/>Rewards): program + tiers keyed by<br/>min_balance_usd and/or a free-text requirement (SoFi direct<br/>deposit, Truist account types) + boost_pct — recorded but<br/>unscored in v1; the optimizer models the no-relationship baseline</i>"]
    REQMEM["<b>required_membership (optional) · max_annual_rewards_usd</b><br/><i>paid membership prerequisite (Sam's Club, Prime,<br/>Robinhood Gold) — unscored by default (the optimizer assumes the<br/>user already holds it) unless card_exclusive: then annual_cost_usd<br/>is scored as an ongoing + year-1 cost (never fee-waived).<br/>max_annual_rewards_usd = card-wide<br/>reward-dollar cap (Sam's Cash $5k/yr), clamped at scoring</i>"]
    FLAGS["<b>benefit_flags[]</b><br/><i>binary perks (lounge_access, primary_rental_insurance) —<br/>recorded but unscored in v1; future filtering</i>"]
    SOURCES["<b>sources[]</b><br/><i>exact URL pasted at the moment a fact enters the file<br/>(by human or AI), with supports[] naming which blocks it backs,<br/>accessed date, added_by — every number traceable to a link<br/>someone actually read</i>"]
    VERIF["<b>verification</b><br/><i>last_verified_date + verified_by + confidence.<br/>sources say where facts came from; verification says when a<br/>human last confirmed the whole file still matches them —<br/>low confidence = drafted, not done</i>"]
end

subgraph META["📚 data/meta/ — shared registries: single source for every cross-card assumption"]
    CATS["<b>categories.yaml</b><br/><i>canonical spend-category enum; will also drive the<br/>spend-entry form, so data and UI can never drift.<br/>'rotating' (quarterly cards) and 'choice' (choose-your-own<br/>cards) are pseudo-categories, banned from spend profiles.<br/>'housing' (rent/mortgage) is explicit_only: no card earns on it<br/>except via an explicit housing category reward (Bilt, fee-free<br/>via its app) — never the base rate — and it's asked in its own<br/>rent/mortgage UI block, not the general grid. A housing reward may<br/>carry an earn_ratio block (Bilt): its points multiplier is a step<br/>function of the Everyday Spend Ratio = everyday-on-card / housing-on-card,<br/>resolved by the optimizer's steering pass</i>"]
    MERCH["<b>merchants.yaml</b><br/><i>canonical merchant enum; each merchant maps to a<br/>category so merchant spend routes out of the right bucket</i>"]
    VALS["<b>point-valuations.yaml</b><br/><i>cents-per-point per program, two anchors:<br/>floor_cpp (guaranteed cash-out) and optimistic_cpp (transfer partners);<br/>the optimizer values points at their ENGAGED AVERAGE —<br/>(floor+optimistic)/2, no user-chosen mode (plan 08) —<br/>one global, user-visible assumption instead of per-card guesses.<br/>redeems_for classifies each currency (flights / hotels / cashback;<br/>empty = merchant-restricted) — a kind counts only when that<br/>redemption path beats the program's own cash-out cpp — so<br/>users can ask the optimizer for specific reward kinds.<br/>loyalty_keys (required on every no-cashback-path program, banned<br/>elsewhere) names the usage-questions brand key(s) whose confirmed<br/>loyalty unlocks the engaged average — an unconfirmed lock-in<br/>currency is valued at floor_cpp (plan 07).<br/>transfer_gateway_required marks currencies whose transfer partners<br/>need a gateway card (Freedom UR needs a Sapphire): the engaged<br/>average applies only when the scored portfolio holds a card with<br/>unlocks_transfers for the program (plan 07 addendum)</i>"]
    QREG["<b>usage-questions.yaml</b><br/><i>the grouped confirmation questionnaire (plan 07): ~10 display<br/>sections, each a 'do you or will you use these' prompt over its<br/>always-visible item checkboxes. Single vocabulary for<br/>credits[].usage_keys, program loyalty_keys, and the<br/>profile's user.confirmed_usage.<br/>Airline/hotel groups carry assumed_reward_kind (flights|hotels):<br/>they render as the separate BRAND-LOYALTY block ('Are you loyal<br/>to any of these?'), and their items are assumed usable for credit<br/>gating whenever the matching kind (or total_value) is in<br/>reward_preferences — a user is presumed to book whichever brand<br/>gives the best value; checking one still means loyalty (optimistic<br/>cpp + full credit capture). Issuer travel portals are<br/>deliberately not asked about — portal use is assumed for every<br/>held card, so the card-level portal key resolves to<br/>statement-descriptors.yaml instead. Item keys must exist in<br/>statement-descriptors.yaml and be referenced by ≥1 card/program —<br/>both directions checked, so users are never shown an option that<br/>can't change a recommendation</i>"]
    DESC["<b>statement-descriptors.yaml</b><br/><i>reward-relevant merchant keywords (extracted from offer_files)<br/>→ common statement-descriptor variants ('DD *DOORDASH',<br/>'AMZN MKTP') — heuristic draft. Read by the validator as the<br/>parent vocabulary for usage-questions item keys, and THE single<br/>registry behind the benefit-usage detector (plan 14): compiled at<br/>startup into the server/statements/ matcher, which matches<br/>transaction descriptors against statement_patterns only —<br/>longest pattern wins, entries marked aggregator_prefix: true<br/>('SQ *', 'PAYPAL *', 'TST*', 'APLPAY') strip the prefix and<br/>re-match the remainder once. A hit counts only when the key is a<br/>usage-questions item; there is no category assignment and no other<br/>matching layer. Never shipped to the browser;<br/>still unread by the optimizer</i>"]
end

subgraph PIPE["✅ Validation pipeline — errors block, warnings nag"]
    VALIDATE["<b>scripts/validate_cards.py</b><br/><i>schema conformance · id=filename · issuer=directory ·<br/>duplicate ids · registry membership · cash⇒program:cash ·<br/>every valuation program has a valid redeems_for list ·<br/>no future verification dates · choice block ⇔ 'choice' category,<br/>≤1 per card, options are real (non-pseudo) categories ·<br/>credits can't use pseudo-categories · amount_points only on<br/>points cards · shared_cap_id groups have ≥2 members agreeing<br/>on period + max_spend_usd · bonus tier spend requirements<br/>exceed the base and strictly ascend · conditional_rate /<br/>base_rate_conditional strictly exceed their baseline rate ·<br/>rotating rewards carry a quarterly cap (uncapped rotating<br/>hard-errors the optimizer) · every merchants.yaml entry routes<br/>to a real (non-pseudo) category · every valuation program has<br/>numeric floor_cpp/optimistic_cpp · plan 07 gating: every credit<br/>carries usage_keys / category / automatic (automatic exclusive<br/>of the other two) · usage_keys / loyalty_keys resolve to<br/>usage-questions items (which resolve to statement-descriptors) ·<br/>assumed_reward_kind on a usage-question group must be<br/>flights|hotels ·<br/>card portal resolves to a statement-descriptors key (portal use<br/>is assumed by the optimizer — portals are not questionnaire items) ·<br/>no-cashback programs require loyalty_keys, cashback programs<br/>may not carry them · portal_only reward lines require a card<br/>portal key · unlocks_transfers only on transfer_gateway_required<br/>programs · statement-descriptors.yaml (plan 14):<br/>aggregator_prefix, when present, must be boolean true</i>"]
    WARNINGS["<b>Warnings (exit 0)</b><br/><i>stale: last_verified_date &gt; 6 months ·<br/>confidence: low · signup bonus past expires ·<br/>promotional credit past expires ·<br/>UNSOURCED: populated block no source supports ·<br/>card file missing from docs/card-backlog.md ·<br/>monthly/quarterly statement credit that is category-only<br/>(short-cycle coupons are almost always merchant-specific) ·<br/>card portal with no portal_only lines · usage-questions item<br/>nothing references —<br/>data-freshness nags that shouldn't block unrelated PRs</i>"]
    CI["<b>.github/workflows/validate-data.yml</b><br/><i>runs on PRs/pushes touching data, AND weekly on cron —<br/>so staleness and expired promos surface<br/>even when nobody is editing</i>"]
end

subgraph OPT["🧮 Optimization engine — deterministic portfolio recommendation (docs/plans/02-optimizer.md)"]
    PROFILE["<b>spend profile YAML (user-authored)</b><br/><i>annual spend per category + optional merchant carve-outs<br/>(sub-buckets of their category, never additive) + user block<br/>(credit_tier, valuation_mode, max_cards, optimize_for,<br/>activates_rotating, reward_preferences — any mix of flights /<br/>hotels / cashback / total_value — accepts_brand_lockin: opt-in to<br/>cards whose rewards redeem with only one company (default false) —<br/>and confirmed_usage: the<br/>usage-questions item keys the user confirmed they use, driving<br/>credit gating and loyalty-aware cpp — for airline/hotel keys<br/>confirmation means brand loyalty; assumed_usage is derived from<br/>reward_preferences by the engine and rejected as a profile input;<br/>replaces the old uses_travel_portal boolean) — keys<br/>validated against the registries at load; see examples/</i>"]
    OPTIMIZE["<b>scripts/optimize.py</b><br/><i>pure function f(dataset, profile, policy constants, --as-of):<br/>identical inputs ⇒ byte-identical output. Buckets partition spend<br/>(explicit_only buckets like housing are flagged: base-rate lines<br/>skip them, so housing earns only through an explicit category<br/>reward, and its spend is excluded from signup-bonus and<br/>credit-unlock feasibility windows — it isn't card-payable; a housing<br/>reward with an earn_ratio block (Bilt) earns a multiplier that is a<br/>step function of the Everyday Spend Ratio — everyday-on-card ÷<br/>housing-on-card — resolved AFTER assignment by a steering pass that<br/>routes the cheapest everyday dollars onto the card up to the tier<br/>maximizing net value, then re-prices housing at that rate or the<br/>per-cycle points floor; earn_ratio cards are context-dependent so<br/>pruning never drops them or prunes on their max tier rate);<br/>reward lines (merchant &gt; category &gt; base per card, caps via<br/>fallback lines, shared_cap_id lines drain one combined pool,<br/>rotating as a capped wildcard diluted to the ~1/N featured-quarter<br/>coverage of each eligible bucket (N = len(ROTATING_ELIGIBLE)),<br/>portal_only lines always kept at<br/>×0.75 — portal use is assumed for every held card) are greedily<br/>assigned by effective rate with deterministic tie-breaks; cpp is<br/>context-aware via effective_cpp: points price at the engaged<br/>average (floor+optimistic)/2 (plan 08 — no valuation mode);<br/>lock-in currencies (loyalty_keys) fall back to floor_cpp unless<br/>loyalty is confirmed, and transfer_gateway_required currencies<br/>fall back to floor unless the scored portfolio holds an<br/>unlocks_transfers card for the program (standalone Freedom Flex is<br/>1cpp cash; paired with a Sapphire it prices at avg 1.5cpp) — gates<br/>surface per card as valuation_note naming the gateway cards, and<br/>the unlocked pairing surfaces as pairing_note ('points pooled<br/>with …'); every per_card block carries its currency<br/>(kind/program/label) so the UI renders the spend → points → $-at-cpp<br/>chain only for points cards; the bundle carries<br/>best_by_size (best portfolio per exact size 1..max_cards) for the<br/>site's escalating best-1 → best-2 → best-3 presentation; dominance pruning never drops a card whose cpp is<br/>portfolio-dependent (its standalone floor understates its paired<br/>value);<br/>credits gate on usage first (usage_keys credits are $0 unless a<br/>key is confirmed — softer CONFIRMED_CREDIT_CAPTURE — or in the<br/>derived assumed_usage set: airline/hotel keys whose group's<br/>assumed_reward_kind is in reward_preferences, kept at the<br/>conservative CREDIT_CAPTURE and echoed as assumed_usage in the<br/>bundle; assumption never unlocks loyalty cpp; keyless credits keep<br/>CREDIT_CAPTURE; full face only<br/>for automatic credits; a usage-gated $0 credit also carries<br/>potential_value = its full annual face (points priced at cpp) —<br/>display-only, value stays 0 so it never enters any total, letting<br/>the UI list 'perks you'd get anyway' the user hasn't confirmed)<br/>then draw a shared per-category<br/>remaining-spend tracker (points credits valued at program cpp;<br/>in_kind always haircut; unlock_spend_usd gated by the same<br/>feasibility rule as bonuses; promo credits past their expires<br/>date valued $0 at --as-of; portal_only credits don't stack —<br/>net counts only the single highest-value portal credit across the<br/>portfolio, the rest deducted, while per-card display keeps full<br/>values); signup bonuses are year-1 only<br/>(mixed points+usd summed, reachable tiers added,<br/>first_year_match = the card's own computed year-1 earnings).<br/>Choose-your-own ('choice') cards expand into one variant per<br/>option the profile spends in; variants of the same card are<br/>mutually exclusive, so the search configures each card optimally<br/>per combination. Exact dominance pruning (plan 02.5) then drops<br/>variants provably unable to appear in an optimal portfolio: a plain<br/>card (no credits/bonus) whose live-bucket rates are pointwise<br/>covered by a rival's uncapped lines at ≤ fees, no reward clamp,<br/>with match-interception + sibling guards; ties prune the larger id.<br/>Exhaustive subset search sizes 1..max_cards, gated on actual work:<br/>Σ C(n,k) ≤ MAX_SCORED_SUBSETS = 2M scored subsets (replaces the<br/>old 80-variant hard stop); approval-tier filter + brand-lock-in<br/>filter (no-cashback-path currencies excluded unless<br/>user.accepts_brand_lockin) + reward-preference<br/>filter (concrete reward_preferences keep only cards whose<br/>currency's redeems_for intersects them; total_value = no filter);<br/>low-confidence / stale / expired-bonus warnings and pruned<br/>variants (with reasons) surface in output, never silently drop. relationship_boost, conditional_rate and<br/>base_rate_conditional are stored but<br/>unscored (unconditional baseline); required_membership is unscored<br/>unless card_exclusive, whose annual_cost_usd joins both fee totals<br/>(and the pruning fee comparison), surfaced per card as<br/>fees.membership_fee_usd; max_annual_rewards_usd clamps<br/>a card's spend earnings (clamp surfaced per card as<br/>reward_cap_clamp in the output so line sums reconcile).<br/>All judgment calls live in one policy-constants block echoed<br/>into every output. stdlib + pyyaml only</i>"]
    TESTS["<b>tests/test_optimizer.py</b><br/><i>golden tests with hand-computed expected values: every seed<br/>card both modes, caps, rotating, choice-variant expansion and<br/>per-combination flips, assumed portal rate (×0.75), credit<br/>usage-gating (unconfirmed $0, confirmed capture tables, automatic<br/>full face, anyOf keys, stacked category gate), assumed usage<br/>(airline/hotel keys unlocked by reward_preferences at conservative<br/>capture, never unlocking loyalty cpp, derived not input, surfaced<br/>in bundle + text), loyalty devaluation<br/>(lock-in cpp floor, bonus/points-credit devaluation, valuation_note<br/>surfacing), confirmed_usage validation + migration error, bonus<br/>feasibility/expiry, portfolio cap competition, synthetic<br/>merchant_rewards/closed_loop fixtures, shared-cap pools,<br/>credit variants (points / in_kind / unlock / every_5_years /<br/>expired-promo $0 / portal_only non-stacking in net), bonus variants (mixed value / tiers /<br/>first-year match), reward-cap clamping (incl. per-card<br/>surfacing in the run() bundle), reward-preference filtering (per-kind,<br/>multi-select union, total_value bypass, empty redeems_for),<br/>byte-determinism, subset-budget gate<br/>(formula + over-budget DataError before scoring), dominance<br/>pruning (worse-clone, extras/cap/fee/clamp blockers, match-<br/>interception guard, tie determinism, run()-level surfacing).<br/>Runs against a frozen fixture copy of the 8 seed cards<br/>(tests/fixtures/data/) so dataset growth never invalidates<br/>the hand-computed goldens</i>"]
end

subgraph WEB["🌐 Product web UI — Vercel deployment + local mode over the same engine (docs/plans/04-tech-stack.md)"]
    SITE["<b>site/ (Vite + React + TypeScript)</b><br/><i>static SPA (Vercel / server static mount) — computes<br/>nothing itself; a minimal history router (lib/router.tsx, no<br/>dependency) serves four real pages in one design language:<br/>/ (the optimizer form), /how-it-works (customer-readable<br/>pipeline walkthrough), /data-sources (per-issuer card table from<br/>GET /api/cards incl. verification status), /assumptions (the<br/>point-valuation table from GET /api/assumptions) — content pages<br/>fetch from the API so they can never drift from the scored data;<br/>API base defaults to same-origin ('') in production builds,<br/>localhost:8000 in dev (VITE_API_URL overrides).<br/>GET /api/config drives every form control<br/>(categories, merchants, questionnaire groups, tiers, defaults) so<br/>NO registry copies are embedded and the UI cannot drift from the<br/>data; groups with assumed_reward_kind render as the separate<br/>Brand-loyalty block (after reward priorities), the rest as the<br/>usage questionnaire. Spend state is integer annual cents with a display-only<br/>monthly|annual toggle (plan 03 §3.2); client validation E1–E5+W1<br/>is an exact mirror of parse_profile, never a superset; profile<br/>emission omits zero categories and sends all user keys.<br/>Statement benefit detection (plan 14 — the gutted successor of the<br/>plan 09/12 spend importer): lib/statements/ uploads each<br/>CSV/OFX/QFX/PDF export to POST /api/statements/parse (4 MB/file<br/>pre-check, client-side SHA-256 dedupe so duplicates never upload,<br/>one retry on 5xx, per-file progress) and receives only the<br/>summary + detected usage-item matches back. The browser sums per<br/>usage key (refunds subtract), annualizes by<br/>365/union-of-covered-days, discloses guessing parse paths<br/>(inferred CSV columns, PDF layout fallback) as info lines, and<br/>renders pre-checked 'Services we spotted' chips — confirming them<br/>merges keys into user.confirmed_usage. Statements never touch<br/>spend: amounts are always entered by hand in the form. Production builds carry a strict self-only CSP meta<br/>(vite.config.ts). lib/ mirrors are vitest-tested; only totals the<br/>user approves on review travel onward to the optimizer</i>"]
    SERVER["<b>server/app.py</b><br/><i>FastAPI wrapping scripts/optimize.py IN-PROCESS (import, not<br/>subprocess) — dataset loaded once at startup. GET /api/config<br/>serves the registries + optimizer constants (and data_last_verified<br/>= max card verification date for the footer trust line) so the<br/>frontend embeds no copies and cannot drift;<br/>GET /api/cards serves per-card summaries (identity, fee, currency,<br/>verification block) for the /data-sources page; GET /api/assumptions<br/>serves the point-valuation table for /assumptions; POST /api/optimize maps a JSON profile<br/>through parse_profile (the single validator — no Pydantic mirror)<br/>+ run(), returning the bundle verbatim; InputError→422,<br/>DataError→500, messages rendered to the user as-is.<br/>POST /api/statements/parse (plan 12; detection-only since<br/>plan 14): one multipart statement file per request →<br/>server/statements/ (detect → csv/ofx/pdf parse — incl.<br/>content-shape CSV column inference and the PDF layout-band<br/>fallback → kind-classify → detect_usage via the<br/>statement-descriptors matcher compiled at startup) →<br/>{summary, matches}: only the purchase/refund transactions whose<br/>descriptor resolves to a usage-questions item return; the full<br/>transaction list never leaves the server. No category assignment,<br/>no keyword/MCC/fuzzy/ML matching, no models, no network/API ever. EPHEMERAL BY POLICY:<br/>bytes live only in the request frame, no debug dumps on this route<br/>ever, errors carry no statement content ({detail, code} taxonomy,<br/>413 oversize) — pinned by tests. as_of defaults<br/>per-request (never stale expiry math); per-run debug dumps to<br/>gitignored server/debug-runs/ LOCAL-ONLY (disabled by policy when<br/>env VERCEL is set — hosted requests are computed and discarded,<br/>never written; OSError also swallowed). Serves site/dist with an SPA<br/>catch-all fallback when built (all-localhost mode).<br/>Contract pinned by tests/test_server_api.py incl. byte-for-byte<br/>golden equivalence with render_json(run(...))</i>"]
    VERCEL["<b>Vercel deployment (vercel.json + api/index.py)</b><br/><i>the no-clone path: static Vite build (site/dist) + the SAME<br/>server/app.py running as one Python serverless function —<br/>api/index.py is a 3-line import shim, so the API has exactly one<br/>definition. Vercel bundles the whole repo into the function<br/>(data/ + scripts/ + server/ ride along; excludeFiles trims<br/>site/, tests, offer files, docs); rewrites route /api/* to the<br/>function and everything else to the SPA's index.html; root<br/>requirements.txt (fastapi + pyyaml + python-multipart + pdfplumber)<br/>is the function's dep list,<br/>server/requirements.txt stays for local dev; the function gets memory: 1024 MB /<br/>maxDuration: 60 s headroom for PDF parsing (pdfplumber is<br/>lazy-imported so optimizer-only cold starts don't pay for it)</i>"]
end

CARD -->|"must conform to"| SCHEMA
IDENTITY ~~~ CURRENCY

CURRENCY -->|"program must be a key in"| VALS
CATREW -->|"category must be a key in"| CATS
MERREW -->|"merchant must be a key in"| MERCH
CREDITS -->|"optional category key in"| CATS
CREDITS -->|"usage_keys must be items in"| QREG
IDENTITY -->|"portal must be a key in"| DESC
VALS -->|"loyalty_keys must be items in"| QREG
QREG -->|"item keys must exist in"| DESC
CLOSED -->|"merchants must be keys in"| MERCH
MERCH -->|"each merchant maps to a category in"| CATS

MERREW -->|"beats when both match"| CATREW
CATREW -->|"beats"| BASE

SOURCES -->|"supports[] must cover every populated block"| WARNINGS

VALIDATE -->|"validates every card against schema + registries"| CARD
VALIDATE -->|"reads"| META
VERIF -->|"staleness &amp; confidence checks"| WARNINGS
SUB -->|"expiry check"| WARNINGS
VALIDATE --> WARNINGS
CI -->|"runs (PR / push / weekly cron)"| VALIDATE
CI -->|"runs unittest suite"| TESTS
TESTS -->|"golden-tests"| OPTIMIZE

PROFILE -->|"category/merchant keys validated against"| META
PROFILE --> OPTIMIZE
SERVER -->|"imports parse_profile + run() in-process"| OPTIMIZE
META -->|"served to the frontend via GET /api/config"| SERVER
SITE -->|"GET /api/config · /api/cards · /api/assumptions · POST /api/optimize"| SERVER
VERCEL -->|"serves the static build of"| SITE
VERCEL -->|"runs as a Python serverless function"| SERVER
CARD -->|"scored by (assumes validator already passed)"| OPTIMIZE
VALS -->|"engaged-average cpp prices every point line;<br/>redeems_for drives the reward-preference filter"| OPTIMIZE
MERCH -->|"routes carve-outs to their parent category"| OPTIMIZE
```

## Reading the diagram

**Data flow in one sentence:** humans hand-write one YAML file per card conforming to the schema's blocks, every cross-card assumption (categories, merchants, point values) lives once in the `meta/` registries and is referenced by key, a validator — run by CI on every data change plus weekly — enforces structure as errors and freshness as warnings, and a deterministic optimizer scores every subset of eligible cards against a user's registry-keyed spend profile to rank portfolios.

**Why the optimizer is a pure function:** `scripts/optimize.py` takes only the dataset, a spend profile, its module-level policy-constants block, and an `--as-of` date — no network, no randomness, no hidden time inputs — so identical inputs produce byte-identical output, every recommendation is reproducible, and every judgment call (credit-capture haircuts, portal discount, rotating 1/N coverage) is echoed into the output where the user can see it. Data-quality problems (`confidence: low`, stale verification, expired bonuses) become per-card warnings in the results rather than silent exclusions, because excluding unverified cards would empty the product today.

**Why the blocks are separate:** each block is a different *kind of value* requiring different math — spend-proportional rates (with caps), fixed periodic credits, a one-time bonus, recurring fees. Flattening them into one rate table or prose is exactly what makes card comparisons unreliable everywhere else. The uniform shape means no card ever needs special-case handling.

**Why registries are separate files:** if every card embedded its own category names or point valuations, two cards could silently disagree ("grocery" vs "groceries"; 1.8cpp vs 2.1cpp for the same points). Keys are validated against the registries, so disagreement is a CI failure, and a valuation change is one edit that repriced every card at once.

**Why errors vs warnings:** structural problems (wrong field, unknown key, id/filename mismatch) mean the data can't be trusted at all — they fail CI. Freshness problems (6-month staleness, `confidence: low`, expired signup promo) mean the data needs a human re-check — they warn without blocking, and the weekly cron guarantees they keep surfacing until fixed.

## Resulting invariants

1. Every card file parses, conforms to the schema, and rejects unknown fields (typos can't hide).
2. `id` matches the filename, `issuer` matches the directory, ids are globally unique.
3. Every category / merchant / points-program key used anywhere resolves to a registry entry.
4. Cash cards always use `program: cash`; points values come only from the shared valuation table.
5. Every card carries a `sources` list pasting the exact URLs its facts came from, each declaring which blocks it supports — populated blocks with no supporting source are flagged.
6. Every card states who verified it, when, at what confidence — and CI re-surfaces anything stale, unverified, unsourced, or past its offer expiry, weekly, forever.
7. Optimizer runs are byte-reproducible: same dataset + profile + `--as-of` ⇒ identical output, with every policy assumption echoed in the run header.
8. In every scored portfolio each dollar of profile spend is assigned to exactly one reward line (closed-loop-only portfolios report unassignable spend as $0, never silently); credits can never exceed the user's real spend in their category; no card is recommended above the user's stated credit tier, and when the user asks for concrete reward kinds (flights / hotels / cashback), no card is recommended whose currency can't redeem for at least one of them.
9. No merchant/service/brand-locked value is ever counted for a user who hasn't confirmed using that service: usage-keyed credits are $0 unless confirmed (with the reason in the output), and lock-in currencies are valued at their cash floor unless brand loyalty is confirmed (surfaced as a per-card valuation note). The one deliberate exception: airline/hotel keys are assumed usable — at the conservative capture rate, echoed as `assumed_usage` — when the user's reward preferences include the matching kind, because such a user books whichever brand gives the best value; the assumption never unlocks loyalty-gated point value.
10. A currency whose transfer partners require a gateway card (Freedom-family UR, Citi TYP, WF Rewards) is priced at its cash floor unless the scored portfolio actually contains that gateway — so a standalone "cash back" card never gets credit for point value it cannot reach, while the Freedom + Sapphire pairing correctly does.
11. Cards whose currency has no cash redemption path (airline miles, hotel points, store credit) are never recommended unless the user explicitly opted into brand restriction (`user.accepts_brand_lockin`) — willingness to be locked to one company is asked, never assumed.
12. Statement parsing is ephemeral and detection-only (plan 14): uploaded statement files are parsed deterministically in the request's memory and discarded — never stored, logged, or debug-dumped (the guard is code + tests, not policy prose), parse errors never echo statement content, and the response carries only the transactions that match a benefit-relevant service — the full transaction list never leaves the server. The user decides which detected services to confirm; spending is always entered manually.
