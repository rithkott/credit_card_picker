# Synthetic statement fixtures

Every file in this directory is **hand-written synthetic data** shaped like an
issuer's export format. **NEVER commit a real statement export here, even
redacted** — real statements are personal financial data and the parsers are
verified against them only locally, outside the repo (plan 09 §Testing).
